# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: tls-gen

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ['3.10', '3.11', '3.12', '3.13', '3.14']
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - name: Show OpenSSL@3 prefix
        if: matrix.os == 'macos-latest'
        run: |
          brew --prefix openssl@3
          ls -l $(brew --prefix openssl@3)
      - name: Show OpenSSL
        run: |
          openssl version
          openssl version -d
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install flake8 pytest
      - name: Lint with flake8
        run: |
          # The GitHub editor is 127 chars wide
          flake8 . --verbose --count --show-source --statistics --max-complexity=10 --max-line-length=127
      - name: Run
        run: |
          make -C basic
          make -C basic info
          make -C basic verify
          make -C basic clean
          make -C one_intermediate
          make -C one_intermediate info
          make -C one_intermediate verify
          make -C one_intermediate clean
          make -C separate_intermediates
          make -C separate_intermediates info
          make -C separate_intermediates verify
          make -C separate_intermediates clean
          make -C two_shared_intermediates
          make -C two_shared_intermediates info
          make -C two_shared_intermediates verify
          make -C two_shared_intermediates clean
      - name: Test
        if: matrix.os == 'ubuntu-latest'
        run: ${{ github.workspace }}/test/basic.sh
